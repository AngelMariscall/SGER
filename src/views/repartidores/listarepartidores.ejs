<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Repartidores | SGER</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            margin: 0;
            font-family: system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, 'Helvetica Neue', Arial, 'Noto Sans', sans-serif;
            background: #f6f7fb;
        }

        .layout {
            display: grid;
            grid-template-columns: 240px 1fr;
            min-height: 100vh;
        }

        .sidebar {
            background: #1f2937;
            color: #fff;
            padding: 16px;
        }

        .sidebar h2 {
            font-size: 18px;
            margin: 0 0 16px;
        }

        .sidebar a {
            display: block;
            color: #cbd5e1;
            text-decoration: none;
            padding: 10px 12px;
            border-radius: 8px;
            margin-bottom: 6px;
        }

        .sidebar a:hover {
            background: #374151;
            color: #fff;
        }

        .content {
            padding: 24px;
        }

        .topbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .topbar .title {
            font-size: 22px;
            font-weight: 600;
            color: #111827;
        }

        .actions {
            display: flex;
            gap: 10px;
        }

        .btn {
            display: inline-block;
            padding: 10px 14px;
            border-radius: 10px;
            text-decoration: none;
            border: 1px solid #e5e7eb;
            background: #fff;
            color: #111827;
        }

        .btn-primary {
            background: #2563eb;
            border-color: #2563eb;
            color: #fff;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            background: #fff;
            border: 1px solid #e5e7eb;
            border-radius: 12px;
            overflow: hidden;
        }

        thead {
            background: #eff6ff;
        }

        th,
        td {
            padding: 12px 14px;
            border-bottom: 1px solid #e5e7eb;
            color: #111827;
        }

        tr:hover {
            background: #fafafa;
        }

        .link {
            color: #2563eb;
            text-decoration: none;
        }
    </style>
</head>

<body>
    <div class="layout">
        <aside class="sidebar">
            <h2>Menú</h2>
            <a href="/dashboard">Dashboard</a>
            <a href="/usuarios">Usuarios</a>
            <a href="/repartidores">Repartidores</a>
            <a href="/pedidos">Pedidos</a>
            <a href="/zonas">Zonas</a>
            <a href="/asignacion">Asignación</a>
            <a href="/auth/perfil">Perfil</a>
            <a href="/auth/logout">Salir</a>
        </aside>

        <main class="content">
            <div class="topbar">
                <div class="title">Repartidores</div>
                <div class="actions">
                    <a class="btn btn-primary" href="/repartidores/nuevo">Nuevo Repartidor</a>
                </div>
            </div>

            <table>
                <thead>
                    <tr>
                        <th>Nombre</th>
                        <th>Zona Asignada</th>
                        <th>Vehículo</th>
                        <th>Placa</th>
                        <th>Status</th>
                        <th>Entregas Realizadas</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <% repartidores.forEach(r=> { %>
                        <tr>
                            <td>
                                <%= (r.usuario_id && r.usuario_id.nombre) || (r.usuario && r.usuario.nombre) %>
                            </td>
                            <td>
                                <%= r.zona_asignada %>
                            </td>
                            <td>
                                <%= r.vehiculo %>
                            </td>
                            <td>
                                <%= r.placa %>
                            </td>
                            <td>
                                <%= r.status %>
                            </td>
                            <td>
                                <%= r.entregas_realizadas %>
                            </td>
                            <td>
                                <a class="link" href="/repartidores/<%= r._id %>">Ver</a>
                                <span> | </span>
                                <a class="link" href="/repartidores/<%= r._id %>/editar">Editar</a>
                                <span> | </span>
                                <form action="/repartidores/<%= r._id %>/eliminar" method="POST" style="display:inline"
                                    onsubmit="return confirm('¿Eliminar este repartidor?');">
                                    <button class="link" type="submit"
                                        style="background:none;border:none;padding:0;color:#ef4444;cursor:pointer">Eliminar</button>
                                </form>
                            </td>
                        </tr>
                        <% }) %>
                </tbody>
            </table>
        </main>
    </div>

</body>

</html>